import { VerificationErrorResult } from "@/lib/types";
const stageNames={claim_extraction:"주장 추출",policy_api:"정책 API",claude_search:"Claude 검색",gemini_search:"Gemini 검색",gemma_analysis:"Gemma 판정",web_search:"웹 검색",local_ai:"판정 엔진"} as const;
export default function FactCardError({result,index}:{result:VerificationErrorResult;index:number}){return <article className="result-card pipeline-error"><div className="result-topline"><span className="claim-number">CLAIM {String(index).padStart(2,"0")}</span><span className="result-status"><i>×</i>검증 오류</span></div><h3>“{result.claim.text}”</h3><div className="pipeline-error-box"><span>{stageNames[result.stage]}</span><strong>{result.title}</strong><p>{result.message}</p><small>{result.retryable?"잠시 후 다시 시도하거나 연결 상태를 확인해 주세요.":"입력 내용을 확인해 주세요."}</small></div></article>}
