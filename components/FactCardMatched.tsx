import { MatchedResult } from "@/lib/types";
export default function FactCardMatched({ result, index }: { result: MatchedResult; index: number }) { const conflict = result.verdict === "conflict"; return <article className={`result-card ${conflict ? "conflict" : "supported"}`}><div className="result-topline"><span className="claim-number">CLAIM {String(index).padStart(2, "0")}</span><span className="result-status"><i>{conflict ? "!" : "✓"}</i>{conflict ? "공식 자료와 상충" : "공식 자료로 확인"}</span></div><h3>“{result.claim.text}”</h3><div className="verdict-box"><strong>{conflict ? "공개된 공식 자료와 다른 내용입니다." : "공식 자료에서 근거를 확인했습니다."}</strong><p>{result.excerpt}</p></div><div className="source-row"><div><span>공식 출처</span><strong>{result.source.agency}</strong><small>{result.source.date}</small></div>{result.source.url && <a href={result.source.url} target="_blank" rel="noreferrer">원문 보기 ↗</a>}</div></article>; }
